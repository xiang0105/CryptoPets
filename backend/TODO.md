# 後端 TODO

- 將 `GET /materials/backpack` 的 `chain-db` 模式接到正式鏈上資料來源。
- 建立素材 ERC-1155 balance 讀取與 indexer 同步。
- 建立寵物 ERC-721 ownership 驗證。
- 統一 API response envelope。
- 補齊 zod validation：body、params、query。
- 補齊 API integration tests。
- 加上 auth、claim reward、market actions 的 rate limit。
- 強化 Supabase migration、seed、RLS。
- 加上 request id、結構化 logging、production monitoring。
