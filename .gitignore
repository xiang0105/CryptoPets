# Logs (日誌檔案)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node modules (極大，絕對不上傳，別人下載後自己 npm install 即可)
node_modules/

# Build outputs (打包後的成果，佈署時才會用到)
dist/
dist-ssr/
*.local

# Editor directories and files (編輯器設定)
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (未來若有金鑰、API Key 等敏感資訊)
.env
.env.local
.env.*.local

# TypeScript (TS 編譯快取)
*.tsbuildinfo

# Project specific(示意圖片、文件等)
docx/