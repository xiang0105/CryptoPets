# 前端 TODO

- 將 `src/data/pets.ts`、`src/data/goodies.ts` 整理為 dev-only adapter。
- 背包頁目前維持空格；後端 `chain-db` 開啟後再渲染素材資料。
- 將 `getMaterialBackpack()` 接入背包頁面的 loading / empty / error flow。
- 補齊市場、背包、寵物頁的 API 狀態處理。
- 補齊 i18n 文字與響應式版面檢查。
- 補齊 type-check、component tests、E2E smoke tests。
- 前端正式資料不得直接信任本地 provider，需以後端 API 與鏈上驗證結果為準。
